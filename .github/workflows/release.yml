name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  prepare:
    uses: ./.github/workflows/release-prepare.yml

  test:
    needs: prepare
    if: ${{ needs.prepare.outputs.skip_release != 'true' }}
    uses: ./.github/workflows/release-test.yml

  package:
    needs:
      - prepare
      - test
    uses: ./.github/workflows/release-package.yml
    if: ${{ needs.prepare.outputs.skip_release != 'true' && needs.test.result == 'success' }}
    with:
      version: ${{ needs.prepare.outputs.version || '' }}

  publish:
    needs:
      - prepare
      - package
    uses: ./.github/workflows/release-publish.yml
    if: ${{ needs.prepare.outputs.skip_release != 'true' && needs.package.result == 'success' }}
    with:
      version: ${{ needs.prepare.outputs.version || '' }}
      tag: ${{ needs.prepare.outputs.tag || '' }}
      is_prerelease: ${{ (needs.prepare.outputs.is_prerelease || 'false') == 'true' }}
      npm_dist_tag: ${{ needs.prepare.outputs.npm_dist_tag || 'latest' }}
      artifact_name: ${{ needs.package.outputs.artifact_name || '' }}
